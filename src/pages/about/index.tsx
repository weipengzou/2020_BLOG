import React from 'react'

interface Props {
    
}

const About = (props: Props) => {
    return (
        <>
            本网站基于 react + antd + typeScript + reacthook...开发
        </>
    )
}

export default About
